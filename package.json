{
  "name": "lazy-garden-api",
  "version": "1.0.0",
  "description": "Lazy Garden API",
  "engines": {
    "node": "20.11.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf ./dist --preserve-root",
    "build": "npm run clean && tsc -p tsconfig.json",
    "dev": "nodemon ./src/index.ts",
    "serve": "npm run build && node ./dist/index.js"
  },
  "author": "Jx32",
  "license": "ISC",
  "dependencies": {
    "@fastify/awilix": "^6.0.1",
    "@fastify/cors": "^10.0.1",
    "@fastify/csrf": "^7.0.0",
    "@fastify/mongodb": "^9.0.1",
    "ajv": "^8.17.1",
    "awilix": "^11.0.1",
    "fast-json-stringify": "^6.0.0",
    "fastify": "^5.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2"
  }
}
